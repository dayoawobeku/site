/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'headless.local',
            '0.gravatar.com',
            '1.gravatar.com',
            '2.gravatar.com',
        ],
    },
}

module.exports = nextConfig
